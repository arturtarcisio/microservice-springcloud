spring:
  application:
    name: mscustomers

server:
  port: 0

eureka:
  client:
    service-url:
      defaultZone: http://course-ms-eureka-user:r-eskmruuerasueso-e-c@localhost:8761/eureka
  instance:
    instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}
